name: milk_tracking
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.20.2
  path_provider: ^2.1.5
  pdf: ^3.12.0
  printing: ^5.14.3
  provider: ^6.1.5+1
  share_plus: ^12.0.2
  url_launcher: ^6.3.2
  file_picker: ^10.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
